# Ưu tiên Java
*.java linguist-language=Java

# ❌ KHÔNG đánh dấu *.html là vendored để nó tính %
# (Có thể giữ CSS và JS là phụ nếu không muốn tính)

*.css linguist-vendored
*.js linguist-vendored

# Nếu muốn, có thể ẩn bớt template phụ
# nhưng vẫn giữ vài file HTML chính để tăng % HTML
src/main/resources/static/** linguist-vendored