# Ưu tiên Java là ngôn ngữ chính
*.java linguist-language=Java

# Ẩn một phần HTML (giao diện phụ), nhưng vẫn để lại HTML chính
src/main/resources/templates/admin/** linguist-vendored
src/main/resources/templates/includes/** linguist-vendored
src/main/resources/static/** linguist-vendored

# KHÔNG đánh dấu *.html toàn cục là vendored để GitHub vẫn nhận HTML
# (Không viết dòng *.html linguist-vendored)
